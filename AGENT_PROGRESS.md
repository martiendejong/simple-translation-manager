# Agent Progress

## 2026-07-20 — task 869e6vpgg
Starting: auto-detect translatable strings in the active theme and plugin templates on install
(`__stm()`/`_e_stm()` calls), seeding the default-language translation from the existing
fallback text, with a re-runnable "Scan for strings" action that never duplicates rows.
